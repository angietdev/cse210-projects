using System;

class Program
{
    static void Main(string[] args)
    {
        GoalManager manager = new GoalManager();
        manager.Start();

        // Creativity: Added a streak system with bonus points for consecutive events.
    }
}